//! pmd-e2e: WebDriver-driven smoke + screenshot harness.
//!
//! All meaningful logic lives in `tests/` (integration tests run against a
//! running `tauri-driver`); this lib target only exists so Cargo has a
//! buildable package target for the crate.
