*.tmp
target/
node_modules/
/build-deps/
/build-appimage/
/build-flatpak/
/dist/
/ui/dist/
/ui/e2e/mock/dist/
tests/screenshots/playwright-results/
tests/screenshots/playwright/
# Generated marketing/AppStream placeholders (scripts/capture-screenshots.sh);
# the metainfo references hosted URLs, so these are upload artifacts, not repo content.
/screenshots/
.playwright-mcp/
.review-screens/

# Claude Code agent worktrees (transient)
.claude/worktrees/

# Local worktrees (transient parallel work)
.worktrees/

# Machine-local cargo build config (sccache)
.cargo/config.toml
.claude/worktrees/
.claude/*.lock
